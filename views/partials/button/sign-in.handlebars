<button class="navitage-button" data-id="{{id}}" data-newsleep="{{button}}">
		{{#if button}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>




    <button type="button" class="sign-in btn btn-primary btn-sm" data-toggle="modal" data-target="#signInButton">
            Sign in
          </button>